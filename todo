1. Create bash files to run the 3 files "buildMeshS.edp", "buildMesh.edp" and "main.edp" in that order automatically, and create additionally the directories for the output.
2. Create julia files that creates the config files.
3. Setup for mogon and run problems.
4. Implement for screened problem.